import React from 'react';

const StaticGameDayTheme = ({ ads }: any) => {
  return (
    <div className="w-full h-screen bg-red-600 flex items-center justify-center">
      <div className="text-white text-center">
        <h1 className="text-6xl font-bold">IT WORKS!</h1>
        <p className="text-2xl mt-4">The White Screen is gone.</p>
        <p className="mt-2">Now we can put the real code back.</p>
      </div>
    </div>
  );
};

export default StaticGameDayTheme;
